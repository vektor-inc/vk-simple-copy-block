{
	"name": "vk-copy-inner-block",
	"license": "GPL-2.0-or-later",
	"main": "build/index.js",
	"scripts": {
		"build": "wp-scripts build",
		"watch": "wp-scripts start",
		"dist": "composer install --no-dev && npx gulp dist && composer install",
		"format": "wp-scripts format",
		"lint:css": "wp-scripts lint-style",
		"lint:js": "wp-scripts lint-js",
		"lint:php": "composer install && composer format && composer phpcs",
		"packages-update": "wp-scripts packages-update",
		"phpunit": "composer install && npx wp-env run phpunit \"phpunit -c /var/www/html/wp-content/plugins/vk-copy-inner-block/phpunit.xml.dist --verbose\"",
		"prepare": "husky install"
	},
	"devDependencies": {
		"@wordpress/scripts": "^25.4.0",
		"gulp": "^4.0.2",
		"husky": "^8.0.1"
	}
}
